export type PatientDetails = {
      name: string;
      serviseType: string;
      date: string;
      formattedAmount: string;
      status: string;
      subscriptionId?: string;
      dueDate?: string;
};